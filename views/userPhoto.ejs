<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Photo</title>
</head>
<body>
        <a class="btn btn-primary" href="/user<%= userId %>">On page</a><br>
    <form action="/user<%= userId %>/photo" method="POST" enctype="multipart/form-data">
        Upload new photo:<br>
        Image:<input type="file" name="gallery_image">
        <button type="submit" class="btn btn-primary">Upload</button>
    </form>
    <div>
        <% for (let i = 0; i < galleryPhotos.length; i++) {%>
        <img src="<%= galleryPhotos[i] %>" width="300px" height="300px">
        <%} %>
    </div>
</body>
</html>